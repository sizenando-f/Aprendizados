#include <iostream>
#include "vetorOrdenado.h"

int main(){
  vetorOrdenado obj;

  obj.insert(3);
  obj.insert(1);
  obj.insert(5);
  obj.insert(0);
  obj.insert(2);

  system("pause");
  return 0;
}
